import AuthForm from '../../components/Login-UI/AuthForm/AuthForm';
import { StyledContainer } from './Login.styled';

const Login = () => {
	return (
		<StyledContainer>
			<AuthForm />
		</StyledContainer>
	);
};

export default Login;
